<Page
    x:Class="Battle_Ground.TitlePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Battle_Ground"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>

            <RowDefinition Height="Auto"/>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>

        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>

            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1*"/>

        </Grid.ColumnDefinitions>
        <TextBlock x:Name="textBlock" TextWrapping="Wrap" Text="Battle Ground" Grid.ColumnSpan="2" Margin="20,0,0,20" TextAlignment="Center" FontSize="40"/>
        <TextBlock x:Name="_txtRules" Grid.Row="1" TextWrapping="Wrap" Text="Rules" Grid.ColumnSpan="2" Margin="10,10,10,17" FontSize="30"/>
        <Button Name="_playBtn" Click="_playBtn_Click" Content="Play" Grid.Row="2" FontSize="20" HorizontalAlignment="Center" Grid.Column="1" Width="304"/>
        <Button x:Name="_scoreBoardBtn" Content="ScoreBoard" HorizontalAlignment="Center" Grid.Row="2" VerticalAlignment="Center" Width="304" Height="39" FontSize="20" Click="showScoreBoard"/>

    </Grid>

</Page>
